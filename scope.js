function add(a, b) {
    const total = a + b;
    // console.log(total)
    if (a > 5) {
        const added = a + b + 30;
        console.log(added)
    }
    else {
        const secondAdd = 90 + a + b
        console.log(secondAdd)
    }
}

add(2, 4)